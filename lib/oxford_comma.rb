def oxford_comma(array)
  if array.length <= 1
    return array
  elsif array.length == 
    array.join("and")
  else
    # last = array.last
    # last_position = array.index(last)
    array[-1] = array[-1]
end